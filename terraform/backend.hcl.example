# Copy to backend.hcl (gitignored), fill in your real bucket, then run:
#   terraform init -backend-config=backend.hcl
#
# The state bucket is created by remote_state.tf as
# soc-copilot-tfstate-<your account id>.
bucket = "soc-copilot-tfstate-123456789012"
